import { Button } from "./ui/button"

const Hero = () =>{
    return (
        <div className="flex items-center justify-center h-screen">
        testing this to see if this will pop up
        </div>
    );
};

export default Hero;